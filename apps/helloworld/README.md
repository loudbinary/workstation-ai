# HelloWorld App

This app is a validation scaffold for workstation-managed apps.

## Purpose

When executed, it prints all environment variables available in the app's `direnv` context so you can validate what was loaded.

## Usage

- Allow direnv: `direnv allow`
- Print variables: `task run`
